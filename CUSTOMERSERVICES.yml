spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/msdb
    password: ENC(AlCQYYeUEDDrX0mmQHVhFHT0sjiGOv9M)
  servlet:
    multipart:
      enabled: true
      max-request-size: 500KB
      max-file-size: 500KB
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update

